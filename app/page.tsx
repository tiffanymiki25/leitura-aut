'use client';
import { useState } from 'react'; import { authClient } from '../lib/auth-client';
export default function Home() { const session=authClient.useSession(); const [register,setRegister]=useState(false); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const submit=async()=>{setBusy(true);setError('');const r=register?await authClient.signUp.email({email,password,name:name||email.split('@')[0]}):await authClient.signIn.email({email,password}); if(r.error)setError(r.error.message||'Não foi possível continuar.'); setBusy(false)}; if(session.isPending)return <main className="center">Abrindo o clube…</main>; if(!session.data)return <main className="auth"><p className="eyebrow">CLUBE DE LEITURA</p><h1>Entre Páginas</h1><p>Um espaço para as nossas leituras e conversas.</p><div className="form">{register&&<input placeholder="Seu nome" value={name} onChange={e=>setName(e.target.value)}/>}<input type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="error">{error}</p>}<button onClick={submit} disabled={busy}>{busy?'Aguarde…':register?'Criar perfil':'Entrar'}</button><button className="link" onClick={()=>setRegister(!register)}>{register?'Já tenho perfil':'Cheguei pelo convite'}</button></div></main>; return <main className="dashboard"><aside><h1>Entre<br/>Páginas</h1><nav><button>Início</button><button>Próxima leitura</button><button>Indicações</button><button>Chat e recados</button><button>Meu perfil</button></nav><button className="out" onClick={()=>authClient.signOut()}>Sair</button></aside><section><p className="eyebrow">BEM-VINDA, {session.data.user.name?.toUpperCase()}</p><h2>O que estamos lendo?</h2><div className="hero"><div><p className="eyebrow">LEITURA ATUAL</p><h3>A biblioteca da meia-noite</h3><p>Matt Haig · Início da leitura: 28 de setembro</p></div></div><div className="cards"><article><p className="eyebrow">PRÓXIMO ENCONTRO</p><h3>25 de outubro · 15h</h3><p>Livraria Ponta de Lápis<br/>Levar uma frase favorita.</p></article><article><p className="eyebrow">MURAL</p><h3>Conversa do clube</h3><p>Quando o Neon estiver conectado, os recados, votos e livros serão carregados aqui em tempo real.</p></article></div></section></main> }
