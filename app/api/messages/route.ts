import { NextResponse } from 'next/server'; import { prisma } from '../../../lib/prisma'; import { currentUser } from '../../../lib/current-user';
export async function POST(request:Request){const user=await currentUser();if(!user)return NextResponse.json({error:'Não autorizado'},{status:401});const {content}=await request.json();if(typeof content!=='string'||!content.trim())return NextResponse.json({error:'Escreva um recado.'},{status:400});const message=await prisma.message.create({data:{content:content.trim().slice(0,800),userId:user.id},include:{user:true}});return NextResponse.json(message,{status:201});}
